<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{{titulo}}</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Arimo:wght@400;500;600;700&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Arimo', sans-serif;
      background: linear-gradient(180deg, rgba(70, 8, 9, 1) 0%, rgba(0, 0, 0, 1) 50%, rgba(70, 25, 1, 1) 100%);
      margin: 0;
      padding: 0;
      color: #e0e1dd;
      min-height: 100vh;
    }

    /* Hero Section */
    .hero {
      position: relative;
      height: 623px;
      width: 100%;
      overflow: hidden;
    }

    .hero-bg {
      position: absolute;
      width: 100%;
      height: 100%;
      top: 0;
      left: 0;
    }

    .hero-image {
      width: 100%;
      height: 100%;
      object-fit: cover;
      opacity: 0.6;
    }

    .hero-overlay {
      position: absolute;
      width: 100%;
      height: 100%;
      top: 0;
      left: 0;
      background: linear-gradient(180deg, rgba(130, 24, 26, 0.7) 0%, rgba(0, 0, 0, 0.5) 50%, rgba(0, 0, 0, 1) 100%);
    }

    .hero-content {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      text-align: center;
      width: 90%;
      max-width: 680px;
    }

    .hero-logo {
      width: 285px;
      height: 194px;
      margin: 0 auto 2rem;
      object-fit: contain;
    }

    .hero-subtitle {
      font-family: 'Inter', sans-serif;
      font-size: 24px;
      line-height: 32px;
      color: #ffdf20;
      margin-bottom: 2rem;
    }

    .hero-buttons {
      display: flex;
      gap: 16px;
      justify-content: center;
      flex-wrap: wrap;
    }

    .btn {
      padding: 12px 24px;
      font-size: 16px;
      font-family: 'Arimo', sans-serif;
      border: none;
      border-radius: 10px;
      cursor: pointer;
      transition: transform 0.2s, box-shadow 0.2s;
      min-width: 168px;
    }

    .btn-primary {
      background: #e7000b;
      color: white;
      box-shadow: 0px 10px 15px -3px rgba(130, 24, 26, 0.5), 0px 4px 6px -4px rgba(130, 24, 26, 0.5);
    }

    .btn-secondary {
      background: #f0b100;
      color: black;
      box-shadow: 0px 10px 15px -3px rgba(115, 62, 10, 0.5), 0px 4px 6px -4px rgba(115, 62, 10, 0.5);
    }

    .btn:hover {
      transform: translateY(-2px);
      box-shadow: 0px 15px 20px -3px rgba(0, 0, 0, 0.4);
    }

    /* About Section */
    .about {
      background: rgba(0, 0, 0, 0.5);
      padding: 80px 16px;
      margin: 0 16px;
    }

    .features-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 24px;
      max-width: 864px;
      margin: 0 auto;
    }

    .feature-card {
      background: transparent;
      border: 2px solid rgba(240, 177, 0, 0.5);
      border-radius: 14px;
      padding: 26px;
    }

    .feature-icon {
      width: 48px;
      height: 48px;
      margin-bottom: 16px;
    }

    .feature-title {
      font-size: 20px;
      line-height: 28px;
      color: #ff6467;
      margin-bottom: 8px;
    }

    .feature-description {
      font-size: 16px;
      line-height: 24px;
      color: rgba(255, 240, 133, 0.8);
    }

    /* Characters Section */
    .characters {
      background: linear-gradient(180deg, #000000 0%, #460809 100%);
      padding: 80px 16px;
    }

    .section-header {
      text-align: center;
      margin-bottom: 64px;
    }

    .section-title {
      font-size: 48px;
      line-height: 48px;
      color: #fb2c36;
      margin-bottom: 16px;
    }

    .section-subtitle {
      font-size: 20px;
      line-height: 28px;
      color: #ffdf20;
    }

    .crew-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 32px;
      max-width: 864px;
      margin: 0 auto;
    }

    .crew-card {
      background: rgba(0, 0, 0, 0.8);
      border: 4px solid;
      border-radius: 16px;
      overflow: hidden;
      transition: transform 0.3s;
    }

    .crew-card:hover {
      transform: translateY(-8px);
    }

    .crew-image-container {
      position: relative;
      height: 256px;
      overflow: hidden;
    }

    .crew-image {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    .crew-overlay {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      opacity: 0.4;
    }

    .crew-icon-badge {
      position: absolute;
      top: 16px;
      right: 16px;
      width: 56px;
      height: 56px;
      background: white;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      box-shadow: 0px 10px 15px -3px rgba(0, 0, 0, 0.1), 0px 4px 6px -4px rgba(0, 0, 0, 0.1);
    }

    .crew-icon {
      width: 28px;
      height: 28px;
    }

    .crew-info {
      padding: 24px;
    }

    .crew-name {
      font-size: 24px;
      line-height: 32px;
      color: #fdc700;
      margin-bottom: 4px;
    }

    .crew-role {
      font-size: 16px;
      line-height: 24px;
      color: #ff6467;
      margin-bottom: 4px;
    }

    .crew-description {
      font-size: 16px;
      line-height: 24px;
      color: rgba(254, 249, 194, 0.8);
    }

    /* Footer */
    .footer {
      background: black;
      border-top: 4px solid #e7000b;
      padding: 52px 16px 40px;
      text-align: center;
    }

    .footer-logo {
      width: 134px;
      height: auto;
      margin: 0 auto 16px;
    }

    .footer-text {
      font-size: 16px;
      line-height: 24px;
      color: #ffdf20;
      margin-bottom: 8px;
    }

    .footer-copyright {
      font-size: 14px;
      line-height: 20px;
      color: rgba(240, 177, 0, 0.6);
    }

    /* Responsive */
    @media (max-width: 768px) {
      .hero {
        height: 500px;
      }

      .hero-logo {
        width: 200px;
        height: auto;
      }

      .hero-subtitle {
        font-size: 18px;
        line-height: 26px;
      }

      .section-title {
        font-size: 32px;
        line-height: 38px;
      }

      .features-grid,
      .crew-grid {
        grid-template-columns: 1fr;
      }

      .about {
        padding: 40px 16px;
      }

      .characters {
        padding: 40px 16px;
      }
    }
  </style>
</head>
<body>
  <!-- Hero Section -->
  <section class="hero">
    <div class="hero-bg">
      <img src="{{images.hero}}" alt="One Piece Hero" class="hero-image" />
      <div class="hero-overlay"></div>
    </div>
    <div class="hero-content">
      <img src="{{images.logo}}" alt="One Piece Logo" class="hero-logo" />
      <p class="hero-subtitle">La aventura épica en busca del tesoro más grande del mundo</p>
      <div class="hero-buttons">
        <button class="btn btn-primary">Explorar Ahora</button>
        <button class="btn btn-secondary">Ver Tripulación</button>
      </div>
    </div>
  </section>

  <!-- About Section -->
  <section class="about">
    <div class="features-grid">
      {{#each features}}
        <div class="feature-card">
          <img src="{{icon}}" alt="{{title}}" class="feature-icon" />
          <h3 class="feature-title">{{title}}</h3>
          <p class="feature-description">{{description}}</p>
        </div>
      {{/each}}
    </div>
  </section>

  <!-- Characters Section -->
  <section class="characters">
    <div class="section-header">
      <h2 class="section-title">Tripulación del Sombrero de Paja</h2>
      <p class="section-subtitle">Conoce a los valientes piratas que navegan hacia la gloria</p>
    </div>
    <div class="crew-grid">
      {{#each crew}}
        <article class="crew-card" style="border-color: {{borderColor}}">
          <div class="crew-image-container">
            <img src="{{imagen}}" alt="{{nombre}}" class="crew-image" />
            <div class="crew-overlay" style="background: linear-gradient(180deg, {{borderColor}}80 0%, {{borderColor}}40 100%)"></div>
            <div class="crew-icon-badge">
              <img src="{{icon}}" alt="Icon" class="crew-icon" />
            </div>
          </div>
          <div class="crew-info">
            <h3 class="crew-name">{{nombre}}</h3>
            <p class="crew-role">{{rol}}</p>
            <p class="crew-description">{{descripcion}}</p>
          </div>
        </article>
      {{/each}}
    </div>
  </section>

  <!-- Footer -->
  <footer class="footer">
    <img src="{{images.logo}}" alt="One Piece Logo" class="footer-logo" />
    <p class="footer-text">Un tributo a la épica serie de Eiichiro Oda</p>
    <p class="footer-copyright">© {{year}} - Sitio web de fans. One Piece es propiedad de Eiichiro Oda y Shueisha.</p>
  </footer>
</body>
</html>